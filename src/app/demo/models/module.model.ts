export interface Module {
    name: String,
    description: String,
    icon: String,
    projectId: String,
    userCreationId: String,
    userModificationId: String,
    createdAt: Date,
    updatedAt: Date
}
